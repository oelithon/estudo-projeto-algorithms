from typing import Tuple


def study_schedule(permanence_period, target_time):
    n = len(permanence_period)
    if permanence_period != Tuple:
        return None
    if target_time == None:
        return None

    return -1
